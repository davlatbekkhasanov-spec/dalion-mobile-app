# Copy to .env.mobile (gitignored) or export before cap:sync
# Production HTTPS — ilova shu manzildan index.html ni yuklaydi (dizayn o‘zgarmaydi)
CAPACITOR_SERVER_URL=https://dalion-mobile-app-production.up.railway.app

# Lokal sinov (Android emulator → host):
# CAPACITOR_SERVER_URL=http://10.0.2.2:3000
