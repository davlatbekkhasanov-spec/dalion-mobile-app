import logging


def setup_logging() -> None:
    # Central logging configuration placeholder for future observability.
    logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s: %(message)s")
